"""Desktop screens for twinscribe.

PySide6 is imported only under this package, so the rest of the library imports and tests
without a GUI toolkit present. The verification screen lives in `verify`; its theme and
timeline widget are in `theme` and `timeline`.
"""
