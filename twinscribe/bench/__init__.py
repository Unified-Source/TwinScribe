"""Bench: public corpora pinned by id, references built from their annotations, every engine
scored with the metrics, and a report. Nothing here reaches the network; the fetch tool
outside the package fills the corpora store the bench reads.
"""
